import {styled} from "@mui/system";
import {StyledLink} from "../Base/styled";

const CustomizedStyledLink = styled(StyledLink)({
  margin: "8px",
  fontSize: "14px",
});

export {CustomizedStyledLink};
