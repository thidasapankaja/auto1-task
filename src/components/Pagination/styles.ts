import {styled} from "@mui/system";
import {StyledLink} from "../Base/styled";

const StyledPaginationOption = styled(StyledLink)({
  margin: "24px",
  fontSize: "14px",
});

export {StyledPaginationOption};
